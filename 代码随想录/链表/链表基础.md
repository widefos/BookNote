---
title: 链表基础
tags:
  - algorithm/linked-list
---

# 链表类型

**单链表**
![[Pasted image 20250325183616.png]]

**双链表**:节点之间双向连接,有前指针和后指针
![[Pasted image 20250325183658.png]]
**循环链表**
![[Pasted image 20250325183730.png]]


# 链表相关操作

**删除节点**：
![[Pasted image 20250326200919.png]]
相关题目：
![[Pasted image 20250326200956.png]]

思路将要删除的节点的前一个节点指针指向删除节点的后一个节点，这里可以新定义一个头结点，头结点的好处在于可以减少对于特殊操作的判断，例如整个链表只有一个节点或者没有节点，使用头结点就不需要进行特判

代码：
```
class Solution {
public:
		ListNode* removeElements (ListNode* head, int val) t
		ListNode* dummyHead = new ListNode(0);// 设置一个虚拟头节点
		dummyHead->next= head;//将虚拟头节点指向head,方便后面执行删除操作
		ListNode* cur = dummyHead;
		while (cur->next != NULL) {
		if (cur->next->val == val) {
		ListNode* tmp = cur->next;
		cur->next = cur->next->hext;  //删除的核心逻辑
		delete tmp;//使用C++时需要手动清理内存
		} else {
		cur = cur->next;
		
	}
	
	head = dummyHead->next;//更新头结点的位置
	delete dummyHead;//使用C++时需要手动清理内存
	return head;

}
```
---

增添节点：
![[Pasted image 20250326201552.png]]

1. 让增添的节点指向下一个节点
2. 将上一个节点指向增添的节点     **如果不使用额外的中间变量这里顺序不能反了**

---

**反转链表：**

![[Pasted image 20250326201947.png]]
**思路**：这里可以利用双指针的思想，两个指针cur表示当前所指节点，pre表示前一个节点，用temp 保存下一个节点的位置，将cur  指向前一个节点，然后更新pre 和cur继续重复上述操作
![[Pasted image 20250326202206.png]]
实现代码：
```
class Solution (
public:
	ListNode* reverseList (ListNode* head) {
	ListNode* temp;// 保存cur的下一个节点
	ListNode* cur = head;
	ListNode* pre = nullptr;
	while (cur) (
		//保存cur的下一个节点,因为接下来要改变cur->next的指向了
		temp = cur->next;
		cur->next = pre; // **
		// 更新pre 和 cur 指针
		pre = cur;
		cur = temp;
return pre;
```

递归写法：
```
class Solution {
public:
	ListNode* reverse (ListNode* pre, ListNode* cur) {
	if(cur == nullptr) return pre;
	ListNode* temp = cur->next;
	cur->next = pre;
	// 可以和双指针法的代码进行对比,如下递归的写法,其实就是做了这两步
	// pre = cur;
	// cur = temp;
	return reverse (cur, temp) ;
	
	ListNode* reverseList (ListNode* head) {
		//和双指针法的初始化是一样的逻辑
		// ListNode* cur = head;
		// ListNode* pre = nullptr;
		return reverse (nullptr, head);
	}

}
```
---
删除倒数第n个节点
![[Pasted image 20250326203009.png]]

分析一下，如果这不是倒数那么很容易，实际上因为我们没法知道总的节点数量，所以无法直接计算出正数第几个节点，这时就可以利用双指针了
操作如下：
1. 先让fast指向正数第n个位置，slow指向第一个位置
2. 将fast 和slow同时向前移动直到fast指向最后一个位置
3. slow指向的值就是倒数第n个节点
原理就就是slow向前移动到了了N-n的位置

代码：
```
class Solution {
public:
	ListNode* removeNthFromEnd (ListNode* head, int.n) {
	ListNode* dummyHead = new ListNode(0);
	dummyHead->next = head;
	ListNode* slow = dummyHead;
	ListNode* fast = dummyHead;
	while (n-- && fast != nullptr) {
	fast = fast->next;
	
	// fast 再提前移动一步,因为需要让slow指向删除节点的上一个节点
	fast = fast->next;
	while (fast != nullptr) {
	fast = fast->next;
	slow = slow->next;
	
	slow->next = slow->next->next;
	return dummyHead->next;

} ;.
```

---
环形链表判断
![[Pasted image 20250326203921.png]]
难点在于如何判断链表是否存在环，以及还需要找到环的入口

## 相关笔记
- [[代码随想录/时间复杂度]]
- [[代码随想录/数组/双指针用法]]
- [[代码随想录/数组/二分查找]]
